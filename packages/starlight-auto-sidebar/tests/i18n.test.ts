import { expect, testFactory } from './test'

const test = testFactory('i18n')

test.describe('updates autogenerated sidebar groups', () => {
  test('uses metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/fr/')

    const items = await page.getSidebarGroupItems('updates')

    expect(items).toMatchSidebar([
      { label: 'a' },
      { label: 'b' },
      {
        label: 'sub-1 (modifié)',
        items: [
          { label: 'sub-1/a' },
          { label: 'sub-1/b' },
          {
            label: 'sub-1/sub-1 (modifié)',
            items: [{ label: 'sub-1/sub-1/a' }, { label: 'sub-1/sub-1/b' }],
          },
        ],
      },
      {
        label: 'sub-2',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
    ])
  })

  test('fallbacks to the default locale metadata', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/ja/')

    const items = await page.getSidebarGroupItems('updates')

    expect(items).toMatchSidebar([
      { label: 'a' },
      { label: 'b' },
      {
        label: 'sub-1 (modified)',
        items: [
          { label: 'sub-1/a' },
          { label: 'sub-1/b' },
          {
            label: 'sub-1/sub-1 (modified)',
            items: [{ label: 'sub-1/sub-1/a' }, { label: 'sub-1/sub-1/b' }],
          },
        ],
      },
      {
        label: 'sub-2',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
    ])
  })
})

test.describe('sorts autogenerated sidebar groups', () => {
  test('uses metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/fr/')

    const items = await page.getSidebarGroupItems('mixed-sort')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      // Manual `order` of 2.
      {
        label: 'sub-2',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
      // Manual `order` of 3.
      {
        label: 'z-sub-1 (modifié)',
        // Sorted by `reverse-slug` for the current locale.
        items: [{ label: 'sub-1/b' }, { label: 'sub-1/a' }],
      },
      { label: 'a' },
      { label: 'b' },
    ])
  })

  test('fallbacks to the default locale metadata', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/ja/')

    const items = await page.getSidebarGroupItems('mixed-sort')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      // Manual `order` of 2.
      {
        label: 'z-sub-1 (modified)',
        items: [{ label: 'sub-1/a' }, { label: 'sub-1/b' }],
      },
      { label: 'a' },
      { label: 'b' },
      {
        label: 'sub-2',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
    ])
  })

  test('sorts by `label` using metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/fr/')

    const items = await page.getSidebarGroupItems('sort-label')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      { label: 'a (modifié)' },
      {
        label: 'a-sub-2 (modifié)',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
      { label: 'z (modifié)' },
      {
        label: 'z-sub-1 (modifié)',
        // Sorted by sidebar label for the current locale.
        items: [{ label: 'a-sub-1/b (modifié)' }, { label: 'z-sub-1/a (modifié)' }],
      },
    ])
  })

  test('sorts by `label` and falls back to the default locale metadata', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/ja/')

    const items = await page.getSidebarGroupItems('sort-label')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      { label: 'a (modified)' },
      {
        label: 'a-sub-2 (modified)',
        items: [{ label: 'sub-2/a' }, { label: 'sub-2/b' }],
      },
      { label: 'z (modified)' },
      {
        label: 'z-sub-1 (modified)',
        items: [{ label: 'a-sub-1/b (modified)' }, { label: 'z-sub-1/a (modified)' }],
      },
    ])
  })

  test('sorts by `reverse-label` using metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/fr/')

    const items = await page.getSidebarGroupItems('sort-reverse-label')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      {
        label: 'z-sub-1 (modifié)',
        // Reverse sorted by sidebar label for the current locale.
        items: [{ label: 'z-sub-1/a (modifié)' }, { label: 'a-sub-1/b (modifié)' }],
      },
      { label: 'z (modifié)' },
      {
        label: 'a-sub-2 (modifié)',
        items: [{ label: 'sub-2/b' }, { label: 'sub-2/a' }],
      },
      { label: 'a (modifié)' },
    ])
  })

  test('sorts by `reverse-label` and falls back to the default locale metadata', async ({ getPage }) => {
    const page = await getPage()
    await page.goTo('/ja/')

    const items = await page.getSidebarGroupItems('sort-reverse-label')

    expect(items).toMatchSidebar([
      // Manual `order` of 1.
      { label: 'c' },
      {
        label: 'z-sub-1 (modified)',
        items: [{ label: 'z-sub-1/a (modified)' }, { label: 'a-sub-1/b (modified)' }],
      },
      { label: 'z (modified)' },
      {
        label: 'a-sub-2 (modified)',
        items: [{ label: 'sub-2/b' }, { label: 'sub-2/a' }],
      },
      { label: 'a (modified)' },
    ])
  })
})

test.describe('updates prev/next links of autogenerated sidebar group pages', () => {
  test('uses metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()

    await page.expectPrevNextLinks(
      'mixed-sort',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/fr/updates/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/fr/mixed-sort/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/fr/mixed-sort/c/', label: 'c' },
            next: { href: '/fr/mixed-sort/sub-2/b/', label: 'sub-2/b' },
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/fr/mixed-sort/sub-2/a/', label: 'sub-2/a' },
            next: { href: '/fr/mixed-sort/sub-1/b/', label: 'sub-1/b' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/fr/mixed-sort/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/fr/mixed-sort/sub-1/a/', label: 'sub-1/a' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            // Prev link label hidden in the file frontmatter.
            prev: null,
            next: { href: '/fr/mixed-sort/a/', label: 'a' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/fr/mixed-sort/sub-1/a/', label: 'sub-1/a' },
            next: { href: '/fr/mixed-sort/b/', label: 'b' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/fr/mixed-sort/a/', label: 'a' },
            next: { href: '/fr/sort-label/c/', label: 'c' },
          },
        },
      ],
      'fr',
    )
  })

  test('fallbacks to the default locale metadata', async ({ getPage }) => {
    const page = await getPage()

    await page.expectPrevNextLinks(
      'mixed-sort',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/ja/updates/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/ja/mixed-sort/sub-1/a/', label: 'sub-1/a' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            prev: { href: '/ja/mixed-sort/c/', label: 'c' },
            next: { href: '/ja/mixed-sort/sub-1/b/', label: 'sub-1/b' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/ja/mixed-sort/sub-1/a/', label: 'sub-1/a' },
            next: { href: '/ja/mixed-sort/a/', label: 'a' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/ja/mixed-sort/sub-1/b/', label: 'sub-1/b' },
            next: { href: '/ja/mixed-sort/b/', label: 'b' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/ja/mixed-sort/a/', label: 'a' },
            next: { href: '/ja/mixed-sort/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/ja/mixed-sort/b/', label: 'b' },
            // Prev link label hidden in the file frontmatter.
            next: null,
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/ja/mixed-sort/sub-2/a/', label: 'sub-2/a' },
            next: { href: '/ja/sort-label/c/', label: 'c' },
          },
        },
      ],
      'ja',
    )
  })

  test('sets prev/next links when sorting by `label` using metadata for the current locale', async ({ getPage }) => {
    const page = await getPage()
    await page.expectPrevNextLinks(
      'sort-label',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/fr/mixed-sort/b/', label: 'b' },
            next: { href: '/fr/sort-label/b/', label: 'a (modifié)' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/fr/sort-label/c/', label: 'c' },
            next: { href: '/fr/sort-label/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/fr/sort-label/b/', label: 'a (modifié)' },
            next: { href: '/fr/sort-label/sub-2/b/', label: 'sub-2/b' },
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/fr/sort-label/sub-2/a/', label: 'sub-2/a' },
            next: { href: '/fr/sort-label/a/', label: 'z (modifié)' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/fr/sort-label/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/fr/sort-label/sub-1/b/', label: 'a-sub-1/b (modifié)' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/fr/sort-label/a/', label: 'z (modifié)' },
            next: { href: '/fr/sort-label/sub-1/a/', label: 'z-sub-1/a (modifié)' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            prev: { href: '/fr/sort-label/sub-1/b/', label: 'a-sub-1/b (modifié)' },
            next: { href: '/fr/sort-reverse-label/c/', label: 'c' },
          },
        },
      ],
      'fr',
    )
  })

  test('sets prev/next links when sorting by `label` and falls back to the default locale metadata', async ({
    getPage,
  }) => {
    const page = await getPage()

    await page.expectPrevNextLinks(
      'sort-label',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/ja/mixed-sort/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/ja/sort-label/b/', label: 'a (modified)' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/ja/sort-label/c/', label: 'c' },
            next: { href: '/ja/sort-label/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/ja/sort-label/b/', label: 'a (modified)' },
            next: { href: '/ja/sort-label/sub-2/b/', label: 'sub-2/b' },
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/ja/sort-label/sub-2/a/', label: 'sub-2/a' },
            next: { href: '/ja/sort-label/a/', label: 'z (modified)' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/ja/sort-label/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/ja/sort-label/sub-1/b/', label: 'a-sub-1/b (modified)' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/ja/sort-label/a/', label: 'z (modified)' },
            next: { href: '/ja/sort-label/sub-1/a/', label: 'z-sub-1/a (modified)' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            prev: { href: '/ja/sort-label/sub-1/b/', label: 'a-sub-1/b (modified)' },
            next: { href: '/ja/sort-reverse-label/c/', label: 'c' },
          },
        },
      ],
      'ja',
    )
  })

  test('sets prev/next links when sorting by `reverse-label` using metadata for the current locale', async ({
    getPage,
  }) => {
    const page = await getPage()

    await page.expectPrevNextLinks(
      'sort-reverse-label',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/fr/sort-label/sub-1/a/', label: 'z-sub-1/a (modifié)' },
            next: { href: '/fr/sort-reverse-label/sub-1/a/', label: 'z-sub-1/a (modifié)' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/c/', label: 'c' },
            next: { href: '/fr/sort-reverse-label/sub-1/b/', label: 'a-sub-1/b (modifié)' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/sub-1/a/', label: 'z-sub-1/a (modifié)' },
            next: { href: '/fr/sort-reverse-label/a/', label: 'z (modifié)' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/sub-1/b/', label: 'a-sub-1/b (modifié)' },
            next: { href: '/fr/sort-reverse-label/sub-2/b/', label: 'sub-2/b' },
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/a/', label: 'z (modifié)' },
            next: { href: '/fr/sort-reverse-label/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/fr/sort-reverse-label/b/', label: 'a (modifié)' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/fr/sort-reverse-label/sub-2/a/', label: 'sub-2/a' },
            // Last sidebar item.
            next: null,
          },
        },
      ],
      'fr',
    )
  })

  test('sets prev/next links when sorting by `reverse-label` and falls back to the default locale metadata', async ({
    getPage,
  }) => {
    const page = await getPage()

    await page.expectPrevNextLinks(
      'sort-reverse-label',
      [
        {
          url: '/c/',
          expected: {
            prev: { href: '/ja/sort-label/sub-1/a/', label: 'z-sub-1/a (modified)' },
            next: { href: '/ja/sort-reverse-label/sub-1/a/', label: 'z-sub-1/a (modified)' },
          },
        },
        {
          url: '/sub-1/a/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/c/', label: 'c' },
            next: { href: '/ja/sort-reverse-label/sub-1/b/', label: 'a-sub-1/b (modified)' },
          },
        },
        {
          url: '/sub-1/b/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/sub-1/a/', label: 'z-sub-1/a (modified)' },
            next: { href: '/ja/sort-reverse-label/a/', label: 'z (modified)' },
          },
        },
        {
          url: '/a/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/sub-1/b/', label: 'a-sub-1/b (modified)' },
            next: { href: '/ja/sort-reverse-label/sub-2/b/', label: 'sub-2/b' },
          },
        },
        {
          url: '/sub-2/b/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/a/', label: 'z (modified)' },
            next: { href: '/ja/sort-reverse-label/sub-2/a/', label: 'sub-2/a' },
          },
        },
        {
          url: '/sub-2/a/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/sub-2/b/', label: 'sub-2/b' },
            next: { href: '/ja/sort-reverse-label/b/', label: 'a (modified)' },
          },
        },
        {
          url: '/b/',
          expected: {
            prev: { href: '/ja/sort-reverse-label/sub-2/a/', label: 'sub-2/a' },
            // Last sidebar item.
            next: null,
          },
        },
      ],
      'ja',
    )
  })
})
